- project:
    name: changemessage
    jobs:
      - 'plugin-{name}-bazel-{branch}-{gerrit-branch}':
          branch:
            - master
          gerrit-branch:
            - master
            - stable-3.2
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - master
            - stable-2.16
