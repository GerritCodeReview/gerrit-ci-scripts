- project:
    name: webhooks
    jobs:
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - stable-2.13
      - 'plugin-{name}-bazel-{branch}-{gerrit-branch}':
          branch:
            - master
          gerrit-branch:
            - master
            - stable-2.14

