- job-template:
    name: 'plugin-owners-{branch}'
    description: "Plugin owners {branch} branch build with BUCK\n"
    wrappers:
      - timeout:
          timeout: 30
          fail: true
      - timestamps
    logrotate:
      numToKeep: 20
      artifactNumToKeep: 20
    properties:
       - authorization:
           anonymous:
             - job-discover
             - job-read
             - job-extended-read
    scm:
      - git:
         remotes:
           - origin:
               url: https://gerrit.googlesource.com/plugins/owners
           - gerrit:
               url: https://gerrit.googlesource.com/gerrit
         branches:
           - origin/{branch}
         submodule:
           recursive: true

    triggers:
      - pollscm: 'H/50 * * * *'

    builders:
      - shell:
         !include-raw gerrit-buck-build-owners-plugin.sh
    publishers:
      - archive:
         artifacts: 'buck-out/gen/plugins/owners*/*.jar'
