- project:
    name: global-refdb
    repo: global-refdb
    organization: GerritForge
    branch:
      - master
      - stable-3.1
      - stable-3.0
    jobs:
      - 'plugin-{name}-gh-bazel-{branch}'
