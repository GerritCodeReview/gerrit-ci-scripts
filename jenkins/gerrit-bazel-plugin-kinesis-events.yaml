- project:
    name: kinesis-events
    repo: kinesis-events
    organization: GerritForge
    jobs:
      - 'plugin-{name}-gh-bazel-{branch}':
          branch:
            - master
          gerrit-branch:
            - stable-3.3
