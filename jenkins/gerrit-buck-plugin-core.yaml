!include gerrit-buck-plugin-template.yaml.inc

- project:
    plugin:
      - cookbook-plugin
      - replication
      - reviewnotes
    branch:
      - stable-2.10
      - stable-2.11
      - master
    jobs:
      - 'plugin-{plugin}-{branch}'

- project:
    plugin:
      - download-commands
    branch:
      - stable-2.11
      - master
    jobs:
      - 'plugin-{plugin}-{branch}'

- project:
    plugin:
      - commit-message-length-validator
      - singleusergroup
    branch:
      - master
    jobs:
      - 'plugin-{plugin}-{branch}'

