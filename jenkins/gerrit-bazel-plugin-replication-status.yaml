- project:
    name: replication-status
    jobs:
      - 'plugin-{name}-bazel-{branch}-{gerrit-branch}':
          branch:
            - master
          gerrit-branch:
            - stable-3.6
            - stable-3.5
      - 'plugin-{name}-verifier'
