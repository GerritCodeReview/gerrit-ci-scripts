- project:
    name: verify-status
    jobs:
      - 'plugin-{name}-{branch}':
          branch:
            - stable-2.13
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - master
