- project:
    name: verify-status
    branch:
      - master
      - stable-2.13
    jobs:
      - 'plugin-{name}-{branch}'

- project:
    name: verify-status-bazel
    branch:
      - master
    jobs:
      - 'plugin-{name}-bazel-{branch}'
