- project:
    name: verify-status
    branch:
      - master
      - stable-2.13
    jobs:
      - 'plugin-{name}-{branch}'

      - 'plugin-{name}-{branch}-bazel':
           branch:
             - master
