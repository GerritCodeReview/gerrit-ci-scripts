- project:
    name: manifest-subscription-mvn
    repo: manifest-subscription
    branch:
      - master
      - stable-2.15
      - stable-2.14
    jobs:
      - 'plugin-{repo}-java-8-mvn-{branch}'
