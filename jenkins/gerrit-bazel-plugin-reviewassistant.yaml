- project:
    name: reviewassistant
    jobs:
      - 'plugin-{name}-bazel-{branch}-{gerrit-branch}':
          extra-plugins: ''
          branch:
            - master
          gerrit-branch:
            - master
