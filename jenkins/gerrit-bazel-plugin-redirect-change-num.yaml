- project:
    name: redirect-change-num
    repo: redirect-change-num
    organization: GerritForge
    jobs:
      - 'plugin-{name}-gh-bazel-{branch}-{gerrit-branch}':
          extra-plugins: ''
          branch:
            - main
          gerrit-branch:
            - master
            - stable-3.8
