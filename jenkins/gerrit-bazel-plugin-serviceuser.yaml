- project:
    name: serviceuser
    jobs:
      - 'plugin-{name}-bazel-{branch}-{gerrit-branch}':
          branch:
            - master
          gerrit-branch:
            - stable-3.8
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - master
            - stable-3.7
            - stable-3.6
