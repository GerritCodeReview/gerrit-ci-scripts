- project:
    name: its-base
    jobs:
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - master
            - stable-3.14
            - stable-3.13
            - stable-3.12
          targets: 'plugins/its-base:its-base plugins/its-base:its_base_tests'
