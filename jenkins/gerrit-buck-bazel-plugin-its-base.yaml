- project:
    name: its-base
    jobs:
      - 'plugin-{name}-{branch}':
          branch:
            - stable-2.13
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - master
            - stable-2.15
            - stable-2.14
          targets: 'plugins/its-base:its-base plugins/its-base:its_base_tests'
