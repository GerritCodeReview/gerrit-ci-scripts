- project:
    name: github
    jobs:
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - master
          extra-artifacts: 'bazel-bin/plugins/github/github-*.jar'
      - 'plugin-{name}-verifier'
