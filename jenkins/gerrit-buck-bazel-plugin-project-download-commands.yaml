- project:
    name: project-download-commands
    jobs:
      - 'plugin-{name}-{branch}':
          branch:
            - stable-2.13
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - master
