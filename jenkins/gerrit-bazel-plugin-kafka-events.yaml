- project:
    name: kafka-events
    jobs:
      - 'plugin-{name}-bazel-{branch}':
          targets: 'plugins/kafka-events plugins/kafka-events:kafka_events_tests'
          branch:
            - master
            - stable-3.2
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - stable-3.1
            - stable-3.0
            - stable-2.16
