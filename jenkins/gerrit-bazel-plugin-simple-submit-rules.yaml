- project:
    name: simple-submit-rules
    jobs:
      - 'plugin-{name}-bazel-{branch}-{gerrit-branch}':
          branch:
            - master
          gerrit-branch:
            - master
            - stable-2.16
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - master
