- project:
    name: reviewit
    branch:
      - master
    jobs:
      - 'app-{name}-{branch}'
    commands: 'mkdir -p ~/.android && ./gradlew assemble'
    artifacts: 'app/build/outputs/apk/*.apk'
