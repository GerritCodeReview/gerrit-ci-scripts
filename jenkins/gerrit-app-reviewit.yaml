- project:
    name: reviewit
    branch:
      - master
    jobs:
      - 'app-{name}-{branch}'
    commands: 'ANDROID_HOME=~/.android-sdk ./gradlew assemble'
    artifacts: 'app/build/outputs/apk/*.apk'
