- project:
    name: events-kafka
    jobs:
      - 'plugin-{name}-bazel-{branch}-{gerrit-branch}':
          targets: 'plugins/events-kafka plugins/events-kafka:events_kafka_tests'
          branch:
            - master
          gerrit-branch:
            - master
            - stable-3.4
      - 'plugin-{name}-bazel-{branch}':
          targets: 'plugins/events-kafka plugins/events-kafka:events_kafka_tests'
          branch:
            - stable-3.3
            - stable-3.2
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - stable-3.1
            - stable-2.16
      - 'plugin-{name}-verifier'
