- project:
    name: rtc
    jobs:
      - 'plugin-its-{name}-{branch}':
          branch:
            - stable-2.11
            - stable-2.10
      - 'plugin-its-{name}-bazel-{branch}':
          branch:
            - master
