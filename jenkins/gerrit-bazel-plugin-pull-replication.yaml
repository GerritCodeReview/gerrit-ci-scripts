- project:
    name: pull-replication
    jobs:
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - master
            - stable-3.2
            - stable-3.1
      - 'plugin-{name}-verifier'
