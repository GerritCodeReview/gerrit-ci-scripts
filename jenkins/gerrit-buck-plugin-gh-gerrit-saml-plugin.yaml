- project:
    name: gerrit-saml-plugin
    repo: gerrit-saml-plugin
    targets: gerrit-saml-plugin
    organization: thesamet
    branch:
      - master
    jobs:
      - 'plugin-{name}-gh-{branch}'

