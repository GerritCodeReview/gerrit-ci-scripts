- project:
    name: analytics
    jobs:
      - 'plugin-{name}-sbt-{branch}':
          branch:
            - stable-3.10
      - 'plugin-{name}-gh-sbt-{branch}':
          branch:
            - master
