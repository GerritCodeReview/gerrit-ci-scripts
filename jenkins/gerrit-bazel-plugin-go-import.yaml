- project:
    name: go-import
    jobs:
      - 'plugin-{name}-bazel-{branch}-{gerrit-branch}':
          branch:
            - master
          gerrit-branch:
            - master
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - stable-3.2
            - stable-2.16
