- project:
    name: automerger
    jobs:
      - 'plugin-{name}-bazel-{branch}-{gerrit-branch}':
          branch:
            - master
          gerrit-branch:
            - master
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - stable-2.16
