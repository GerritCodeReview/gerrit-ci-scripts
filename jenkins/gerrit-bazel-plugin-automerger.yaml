- project:
    name: automerger
    jobs:
      - 'plugin-{name}-bazel-{branch}-{gerrit-branch}':
          branch:
            - master
          gerrit-branch:
      - 'plugin-{name}-bazel-{branch}':
          branch:
            - master
